# PRD：今日热搜网站（PRD.md）

> 训练营 Day 4 产出，基于 Day 3 的 `research.md`。
> 本文只定义产品**做什么、做成什么样、怎么算合格**，**不写任何代码**。
> 项目定位：一个把多个平台热搜榜单聚合到一个页面看的**纯静态**轻量网站，零基础 28 天内做成。

---

## 一、产品概述

**今日热搜**是一个个人版轻量热搜聚合页。用户在**一个网页**里就能看到 2–3 个平台（如微信、微博、百度、抖音等）的实时热搜榜单，按平台切换、看排名/标题/热度，点开看详情，并能把自己关心的条目**收藏或加备注**。

核心价值来自 research.md 的核心洞察：**不必自己造数据**。初期用本地示例数据把页面做出来，后期再接公开 API；全程不抓取、不存储原文、不商用，只做"信息中介 + 整理者"。

- **形态**：纯静态站点（HTML + CSS + JavaScript），无后端、无数据库。
- **部署**：GitHub Pages / Vercel 等静态托管，免费、零运维。
- **MVP 上线目标**：训练营第 7 天（D7）。

---

## 二、目标用户

| 类型 | 是谁 | 典型场景 |
|---|---|---|
| **主要用户** | 想"一眼看完全网在热什么"的普通个人用户 | 早上打开网页，30 秒扫完各平台热点 |
| **次要用户** | 内容运营 / 自媒体 / 学生 / 研究者 | 找选题、追热点、做内容参考 |
| **非目标用户** | 需要深度数据分析、跨平台算法推荐的专业机构 | 本期不做（见第八节） |

**关键约束**：目标用户是**零基础个人开发者自己用 + 给同学看**，所以全部功能都必须在"不写后端、不接数据库、不登录"的前提下可用。

---

## 三、MVP 功能（本期 D7 上线，必须做）

> 每个功能都按「用户动作 → 可观察结果」描述，不写代码。

### 3.1 首页热搜列表
- **用户动作**：打开网站首页。
- **可观察结果**：页面默认展示至少 2 个平台的热搜列表；每条热搜至少包含**排名、标题、热度值**三个要素；列表按排名从上到下排列。
- **用户动作**：点击顶部的平台切换标签（如"微博 / 百度 / 抖音"）。
- **可观察结果**：列表内容切换为该平台的热搜，排名/标题/热度同步更新，当前选中的标签有高亮标识。

### 3.2 详情查看
- **用户动作**：点击列表中的任意一条热搜。
- **可观察结果**：进入/弹出该条的**详情视图**，能看到：排名、标题、热度值、来源平台、以及"去原平台查看"的入口按钮；无需离开本站即可看到概要信息。
- **用户动作**：在详情视图点击"去原平台查看"。
- **可观察结果**：在新浏览器标签打开该热搜在**原平台**的对应页面（如微博热搜原话题页）。

### 3.3 收藏 / 备注动作（本期唯一互动功能）
> 范围说明：research.md 原把"收藏"列入本期不做；此处按 Day 4 指令保留一个**轻量版**收藏/备注，因为它用浏览器本地存储实现，**不需要登录、不需要后端、不需要数据库**，与"无后端"原则不冲突。

- **用户动作**：在列表项或详情视图点击"收藏"星标。
- **可观察结果**：该条标记为已收藏（星标变实心），并出现一个"我的收藏"入口；操作后即时可见状态变化。
- **用户动作**：对某条热搜点击"加备注"并输入文字、保存。
- **可观察结果**：备注文字与该条绑定保存，详情视图或收藏区能再次看到这条备注。
- **用户动作**：刷新页面或关闭浏览器后重新打开网站。
- **可观察结果**：之前收藏的条目和写的备注**仍然保留**（数据存在本机浏览器，不依赖账号）。
- **用户动作**：在"我的收藏"里点击某条或删除收藏/备注。
- **可观察结果**：该条从收藏区移除，状态同步更新。

---

## 四、后续功能（不在 MVP，明确列出）

以下功能**本期（D7 MVP）不做**，列入后续迭代：

- **用户系统**：登录 / 注册 / 个人中心（MVP 用本地存储代替账号）。
- **复杂推荐 / 个性化推送**："猜你喜欢"类算法，本期不做。
- **付费 / 商业化 / 广告位**：不接广告、不变现。
- **更多平台接入**：MVP 只接 2–3 个平台，后续扩展到更多。
- **真实数据接入**：MVP 用示例数据；后续接公开 API / 各平台开放热搜接口（D15 后）。
- **自动刷新**：MVP 用手动刷新按钮；后续接 API 后做定时自动刷新。
- **历史回溯 / 热度趋势图**：进阶分析功能。
- **互动社区**：评论 / 点赞 / 订阅 / 分享等社交功能。
- **移动 App / 小程序**：只做响应式网页，不做原生应用。
- **多语言 / 国际化**：只做中文。

---

## 五、页面与交互要求

### 5.1 页面结构
- **首页**：顶部平台切换标签 + 热搜列表（每条：排名、标题、热度值、收藏星标）+ "我的收藏"入口 + 手动刷新按钮。
- **详情视图**：排名、标题、热度值、来源平台、"去原平台查看"按钮、收藏/备注操作区。
- **我的收藏页/区**：列出已收藏条目及其备注，支持取消收藏/删除备注。
- **空状态**：收藏区无内容时显示"还没有收藏，去首页挑几条吧"之类提示。

### 5.2 交互要求
- 平台切换、收藏、备注、刷新均为**点击**即可完成，无需键盘输入主流程。
- 备注输入使用单行/多行文本框 + 保存按钮。
- 所有跳转外站都用新标签打开，不丢失本站页面。
- **响应式**：在手机和电脑浏览器上都能正常阅读（不要求原生 App）。

### 5.3 视觉与可读性
- 列表清晰、排名数字醒目；热度值可用颜色/进度条辅助区分。
- 当前选中平台标签有明显视觉区分。
- 整体风格简洁、无广告、加载快（对齐 research 中"今日热榜"的优点）。

---

## 六、数据字段

### 6.1 热搜条目 HotItem
| 字段 | 说明 | 示例 |
|---|---|---|
| `rank` | 排名（整数） | 1 |
| `title` | 热搜标题 | "今日热搜是什么" |
| `heat` | 热度值（字符串/数字，由来源决定） | "523 万" / 982341 |
| `platform` | 来源平台名 | "微博" |
| `url` | 原平台对应页面链接 | https://weibo.com/... |

### 6.2 收藏 / 备注 Collection（仅存本机浏览器）
| 字段 | 说明 |
|---|---|
| `itemRef` | 关联的热搜条目标识（平台 + 排名 或 标题） |
| `note` | 用户写的备注文字（可为空） |
| `createdAt` | 收藏/备注创建时间 |

### 6.3 数据来源策略
- **Phase 1（D7 MVP）**：本地示例数据（一个手写数据文件，放几条示例热搜先把页面撑起来）。
- **Phase 2（D15 后）**：接**免费公开 API / 各平台开放热搜接口**，**绝不自建爬虫**。
- 原则：不抓取、不存储原文、不商用。

---

## 七、异常状态

| 场景 | 用户看到什么 | 系统应如何处理 |
|---|---|---|
| **数据加载失败 / 网络异常** | 页面不白屏，显示"暂时拿不到数据"提示 + "重试"按钮 | 可回退展示示例数据，或提示错误并允许手动重试 |
| **点击"去原平台查看"但外站打不开** | 新标签打开失败或无响应 | 提示"原平台可能暂不可用"，不影响本站其他功能 |
| **浏览器禁用本地存储（隐私模式等）** | 收藏/备注操作提示"当前无法保存" | 明确告知功能受限，不报错崩溃 |
| **某平台接口临时不可用（Phase 2）** | 该平台列表显示"数据暂不可用" | 其余平台正常展示，不整体崩溃 |
| **收藏区为空** | 显示空状态引导文案 | 引导用户去首页收藏 |
| **示例数据缺失 / 格式错误** | 提示"示例数据异常" | 给出明确报错，便于排查 |

---

## 八、本期不做（砍掉的功能及原因）

> 本节直接回答"今天在 PRD 里砍掉了什么、为什么砍"——这是 Day 4 要掌握的点。

| 砍掉的功能 | 为什么砍（原因） |
|---|---|
| 后端服务 / 数据库 | 零基础阶段做不动；纯静态站点已能满足 MVP，且 Day 23 才引入 `.env` 做简单配置 |
| 用户系统（登录/注册/个人中心） | MVP 用浏览器本地存储代替账号，避免后端与密码等复杂度 |
| 评论 / 点赞 / 订阅 / 个性化推荐 | 属于"互动 + 算法"，维护成本高，且 research 指出热搜是强治理场域，个人项目不碰 |
| **收藏 / 备注（原研究列入不做）** | **例外保留轻量版**：改为浏览器本地存储实现，不需后端/账号，与"无后端"原则兼容；若坚持完全不做，可在评审时再砍 |
| 自建爬虫抓取 | 法律风险 + 维护成本；一律用公开 API 或示例数据 |
| 移动 App / 小程序 | 只做响应式网页即可覆盖手机访问，原生应用成本过高 |
| 推荐算法 / "猜你喜欢" | 不建用户画像、不做算法，避免复杂与合规风险 |
| 商业化 / 广告位 | 不接广告、不变现，保持纯净 |
| 历史回溯 / 热度趋势图 | 进阶分析，依赖真实数据与存储，以后再说 |
| 多语言 / 国际化 | 只做中文，聚焦核心 |

**一句话总结砍法**：凡是"需要后端、需要账号、需要算法、需要爬虫、需要花钱"的，一律本期砍掉；只保留"一个静态页面 + 本地收藏"这种零基础能扛住的最小闭环。

---

## 九、验收标准

> 以下任一条都应是"打开网站就能当场验证"的。Day 4 收尾截图需包含本节。

- **AC1** 首页默认展示至少 2 个平台的热搜列表，每条至少含「排名、标题、热度值」。
- **AC2** 顶部平台切换标签可点击，切换后列表内容随之更新，当前标签有高亮。
- **AC3** 点击任意热搜条目可在站内查看详情（排名/标题/热度/来源平台/原链接入口），不强制跳转即可看概要。
- **AC4** 详情视图/列表项提供"去原平台查看"按钮，点击后在新标签打开原站页面。
- **AC5** 提供收藏或备注入口；操作后该条标记为已收藏 / 备注已保存，状态即时可见。
- **AC6** 收藏与备注使用浏览器本地存储，**刷新或重开浏览器后仍保留**，全程无需登录。
- **AC7** 整个 MVP 为纯静态站点：**无后端服务、无数据库、无用户系统**；部署后任意设备浏览器可访问。
- **AC8** 未登录状态下全部 MVP 功能可用。
- **AC9** 数据加载失败时页面不白屏，给出明确提示与重试/示例数据回退。
- **AC10** 仓库不包含 `.env`、密钥、`node_modules`（沿用 Day 2 的 `.gitignore`）。

---

## 附：与 research.md 的关系与范围变更

- 本 PRD 基于 `research.md`（Day 3）撰写，产品定位、目标用户、数据策略均继承自研究结论。
- **唯一范围变更**：research 将"收藏"列入本期不做，本 PRD 按 Day 4 指令保留**轻量本地收藏/备注**（localStorage 实现），原因见第八节。
- 技术选型（纯静态、vibe coding、静态托管）与 research 第四节一致，技术细节留到 Day 5 再定。
